import 'package:flutter/material.dart';

class Racha extends StatelessWidget {
  const Racha({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Pantalla Menu")));
  }
}
